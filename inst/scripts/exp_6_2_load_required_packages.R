# Required packages:
# install.packages("cluster")
# install.packages("factoextra")
# install.packages("ggplot2")
# install.packages("pheatmap")
# install.packages("reshape2")

# Title: Load Required Packages
# Aim: To load the necessary packages into the R environment.
# Procedure: Step-by-Step Execution 1.   Call library(): Each library() call searches the installed packages and loads the specified package into the active R session. 2.   Load ggplot2 and cluster: These provide plotting functions and K-means/hierarchical clustering algorithms respectively. 3.   Load factoextra and reshape2: factoextra enables visual cluster analysis; reshape2 provides data reshaping functions. 4.   Load pheatmap: pheatmap is loaded last, enabling the creation of annotated heatmaps with built-in hierarchical clustering.
# Result: All 5 packages loaded into the R environment successfully.

library(cluster)
library(factoextra)
library(ggplot2)
library(pheatmap)
library(reshape2)

# Name: Kasinath C A  Reg No: 24MID0124
