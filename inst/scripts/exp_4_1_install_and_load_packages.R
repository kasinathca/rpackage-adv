# Required packages:
# install.packages("dplyr")
# install.packages("gapminder")
# install.packages("ggplot2")
# install.packages("tidyr")

# Title: Install and Load Packages
# Aim: To install and load the necessary packages: tidyr, dplyr, ggplot2, and gapminder.
# Procedure: Step-by-Step Execution 1.   Install Packages: The install.packages() function downloads and installs tidyr, dplyr, ggplot2, and gapminder from CRAN. 2.   Comment Header: The first line uses # to write your name and registration number as a non-executable comment. 3.   Load Libraries: The library() function loads each installed package into the active R session for use. 4.   Verify Success: If no error messages appear after each library() call, all packages are loaded and ready.
# Result: All required packages (tidyr, dplyr, ggplot2, gapminder) installed and loaded successfully.

library(dplyr)
library(gapminder)
library(ggplot2)
library(tidyr)

# Name: Kasinath C A  Reg No: 24MID0124
# Install packages (run once)

# Load packages
