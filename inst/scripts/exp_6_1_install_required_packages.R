# Required packages:
# None

# Title: Install Required Packages
# Aim: To install the required R packages: ggplot2, cluster, factoextra, reshape2, pheatmap.
# Procedure: Step-by-Step Execution 1.   Run Install Commands: Each install.packages() call downloads its package (ggplot2, cluster, factoextra, reshape2, pheatmap) from CRAN. 2.   Wait for Completion: Each installation prints progress and ends with OK, confirming successful download and installation. 3.   Run Once Only: These commands only need to be executed once per machine; subsequent sessions use library() to load them. 4.   Verify Installation: Absence of error messages after each command confirms all five packages are installed and ready.
# Result: All 5 packages (ggplot2, cluster, factoextra, reshape2, pheatmap) installed successfully.

# Name: Kasinath C A  Reg No: 24MID0124
# Run once to install
